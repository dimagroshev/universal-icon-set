```jsx-vue [React]
  import { {{ params?.name || 'PlayLine' }} } from '@123done/universal-icon-set-react';

  const App = () => {
    return (
      <{{ params?.name || 'PlayLine' }} />
    );
  };

  export default App;
```
